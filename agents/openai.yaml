interface:
  display_name: "Code Doctor"
  short_description: "Evidence-backed review and repair with controlled restructuring"
  default_prompt: "Use $code-doctor to inspect the change, choose the least invasive capable mode, fix real issues when requested, validate locally, and report concise evidence."

policy:
  allow_implicit_invocation: true
